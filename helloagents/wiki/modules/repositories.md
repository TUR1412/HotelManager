# Repository 模块

## 目的
集中管理 SQL 查询与数据映射，隔离数据库细节，提升可维护性与可审计性。

## 模块概述
- **职责:** SQL 查询/更新 + 参数化 + 数据映射（Row → Domain）
- **状态:** 🚧开发中
- **最后更新:** 2025-12-31

## 规范
- 所有用户输入必须通过参数绑定（`?`）进入 SQL。
- 允许的动态片段必须受控（通过 `sql.py` 收敛 where 子句构建）。

