# Service 模块

## 目的
封装业务规则、输入校验与事务边界，保证“同一规则在不同入口下行为一致”。

## 模块概述
- **职责:** 校验 + 业务规则 + 事务原子性（并发安全）
- **状态:** ✅稳定
- **最后更新:** 2025-12-31

## 规范
- 写操作在必要时使用 `BEGIN IMMEDIATE` 保护“冲突检测 + 写入”原子性。
- 导出快照（`export_snapshot`）只聚合数据与格式化时间，不引入业务副作用。

