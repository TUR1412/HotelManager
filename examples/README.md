# 示例脚本（Examples）

这些脚本用于“快速走一遍典型流程”，验证项目在真实使用路径上可运行、输出友好、数据可查询。

> 注意：脚本只包含有限任务（不会启动任何长期驻留服务）。

## Windows（PowerShell）

```powershell
pwsh -NoLogo -NoProfile -File .\examples\demo.ps1
```

## macOS / Linux（bash）

```bash
bash ./examples/demo.sh
```

